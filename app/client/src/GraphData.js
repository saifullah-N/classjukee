// import React, { useEffect,useState } from 'react'
// import openSocket from 'socket.io-client';
// var socket = openSocket("http://localhost:8080")

// var gData;
//   let arr = [];
//   socket.on('mach-1', (peices, time) => (arr.push({ machId:'mach-1',peices:peices,time: time})));
//   socket.on('mach-2', (peices, time) => (arr.push({ machId: 'mach-2', peices: peices, time: time })));
//   socket.on('mach-3', (peices, time) => (arr.push({ machId: 'mach-3', peices: peices, time: time })));
//   socket.on('mach-4', (peices, time) => (arr.push({ machId: 'mach-4', peices: peices, time: time })));
//   socket.on('mach-5', (peices, time) => (arr.push({ machId: 'mach-5', peices: peices, time: time })));
//   socket.on('mach-6', (peices, time) => (arr.push({ machId: 'mach-6', peices: peices, time: time })));
//   console.log(arr);
// gData=arr
// arr=[]




// export default gData